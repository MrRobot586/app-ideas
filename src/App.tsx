import React from "react";

import Bin2Dec from "./components/Bin2Dec";

type Props = {};

const App = (props: Props) => {
    return (
        <div>
            <Bin2Dec />
        </div>
    );
};

export default App;
